# AIテストランチャー

## PMへ：これだけ読めばOK

### 🎯 やること
複数のAIにテスト問題を解かせて結果を収集する

### 📋 手順（3ステップ）

#### 1. テストフォルダを作る
```bash
# トップレベルに作成（old-testsではない！）
cp -r template/ test-20250813-[テスト名]/
```

#### 2. AIを起動する
```powershell
cd auto-claude-start
.\test_launcher_v2.ps1 -AIName "AI1"
```
必要に応じて `-TaskType "addition"` などを追加

#### 3. 結果を回収
- AIが `/tmp/test_answer_*.txt` に回答を保存
- それを `test-[日付]/test-answers/` にコピー

### ⚠️ 重要
- `auto-claude-start/config.json`の`current_test`を更新すること
  ```json
  "current_test": "test-20250813"  ← 新しいテストフォルダ名に変更
  ```
- 詳細は `docs/` フォルダ内のドキュメント参照

### 📂 フォルダ構造
```
claude-test-100/
├── README.md          ← 今読んでいるファイル
├── template/          ← コピーして使う
├── auto-claude-start/ ← ツール（test_launcher_v2.ps1）
├── docs/             ← 詳細ドキュメント
├── old-tests/        ← 完了した過去のテスト
└── test-*/           ← 現在実行中のテスト（ここに作成！）
```

以上