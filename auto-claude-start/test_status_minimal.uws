// 最小限のSTATUS関数テスト

// UWSCのウィンドウIDを取得
id = GETID("UWSC")

// ファイルに結果を書く
fid = FOPEN("C:\Users\user\claude-test-100-v0.2\auto-claude-start\status_test.txt", F_WRITE)
FPUT(fid, "ID: " + id)

// ST_TITLEをテスト
title = STATUS(id, ST_TITLE)
FPUT(fid, "TITLE: " + title)

// ST_CLASSをテスト
class = STATUS(id, ST_CLASS)
FPUT(fid, "CLASS: " + class)

FCLOSE(fid)